# Development Run Instructions

Run a local web server from the repository root:

```bash
python3 -m http.server 8000
```

Then open this URL in your browser:

- http://localhost:8000/Dashboard_Tabbed.html
